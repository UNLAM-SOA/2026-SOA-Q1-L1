Solo se puede generar, leer o modificar código dentro de la carpeta `Android/Proyecto_con_Vibecoding`.

Cualquier otra carpeta del repositorio está prohibida de ser leída o escrita (por ejemplo `Android/Proyecto_sin_Vibecoding`, `Embebido`, y cualquier otra carpeta fuera de `Android/Proyecto_con_Vibecoding`). No uses herramientas de lectura, búsqueda ni escritura sobre esas rutas, ni siquiera para consultar contexto o referencia.

Esta restricción no aplica a este mismo archivo (`CLAUDE.md`, en la raíz del repositorio), que sí puede leerse ya que contiene estas instrucciones.